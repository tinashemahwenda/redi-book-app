import 'package:flutter/material.dart';

class BookLists extends StatelessWidget {
  const BookLists({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('List Coming Soon'),
      ),
    );
  }
}
